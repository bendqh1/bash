## My aliases

```shell
alias war="cd $HOME && cd www"
alias wad="cd $HOME && cd www/benaharoni.com"
alias dcr="drush cache:rebuild"
```

There can also be exported functions, such as `drupal_upgrade(CONTENT)` to which I call in a similar way `drupal upgrade`.
